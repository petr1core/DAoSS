# Рефакторинг Flowchart Editor

## 📋 Обзор

Проект был полностью рефакторен из одного большого файла `app.js` (2920 строк) в модульную архитектуру с TypeScript типизацией.

## 📁 Новая структура

```
src/
├── types/              # TypeScript типы и интерфейсы
│   ├── flowchart.ts    # Типы для блок-схемы (Node, Connection, State)
│   └── parser.ts       # Типы для парсеров (Pascal SPR, C AST)
│
├── parsers/            # Парсеры кода в блок-схему
│   ├── pascalParser.ts        # Парсер Pascal (SPR формат)
│   ├── cParser.ts             # Парсер C/C++ (AST формат)
│   ├── expressionConverter.ts # Конвертация AST выражений в строки
│   ├── parserUtils.ts         # Общие утилиты парсеров
│   └── index.ts               # Главный модуль парсеров
│
├── utils/              # Утилиты
│   ├── toast.ts        # Toast уведомления
│   ├── date.ts         # Форматирование дат
│   ├── geometry.ts     # Геометрические вычисления (порты, позиции)
│   ├── nodeUtils.ts    # Утилиты для узлов
│   ├── history.ts      # Работа с историей изменений
│   └── index.ts
│
├── services/           # API сервисы
│   ├── api.ts          # Базовый API клиент
│   ├── parserService.ts # Сервис для вызова парсера API
│   └── index.ts
│
├── state/              # Управление состоянием
│   └── flowchartStore.ts # FlowchartStore класс
│
├── hooks/              # React hooks
│   ├── useFlowchartStore.ts  # Hook для работы со store
│   ├── useFlowchartParser.ts # Hook для парсинга кода
│   └── index.ts
│
├── rendering/          # Модули рендеринга
│   ├── connectionRenderer.ts # Рендеринг соединений
│   ├── nodeRenderer.ts       # Рендеринг узлов
│   ├── panelRenderer.ts      # Рендеринг панелей (info, comments, history)
│   └── index.ts
│
├── handlers/           # Обработчики событий
│   ├── nodeHandlers.ts       # Обработка узлов (add, edit, drag)
│   ├── connectionHandlers.ts # Обработка соединений
│   ├── canvasHandlers.ts     # Canvas panning и zoom
│   └── index.ts
│
└── index.ts            # Главный экспорт всех модулей
```

## ✅ Преимущества

1. **Типизация** - Все модули используют TypeScript типы
2. **Модульность** - Каждый модуль решает свою задачу
3. **Переиспользование** - Модули можно использовать независимо
4. **Тестируемость** - Функции легко покрыть тестами
5. **Поддерживаемость** - Легче находить и исправлять проблемы

## 🚀 Использование

### Базовое использование парсеров

```typescript
import { parseJsonToFlowchart } from './parsers';
import type { ParserLanguage, PascalProgram, ASTProgram } from './types/parser';

const pascalData: PascalProgram = { /* ... */ };
const result = parseJsonToFlowchart(pascalData, 'pascal');

// result.nodes - массив узлов
// result.connections - массив соединений
```

### Использование React hooks

```typescript
import { useFlowchartStore, useFlowchartParser } from './hooks';

function MyComponent() {
  const store = useFlowchartStore();
  const parser = useFlowchartParser();

  const handleParse = async () => {
    const result = await parser.parseCode(code, 'pascal');
    if (result) {
      store.setNodes(result.nodes);
      store.setConnections(result.connections);
    }
  };

  return (
    <div>
      {store.nodes.map(node => (
        <div key={node.id}>{node.text}</div>
      ))}
    </div>
  );
}
```

### Использование утилит

```typescript
import { showToast } from './utils/toast';
import { formatDate, getTimeAgo } from './utils/date';
import { getPortPosition } from './utils/geometry';

showToast('Успешно!', 'success');
const dateStr = formatDate(new Date());
const timeAgo = getTimeAgo(new Date());
const portPos = getPortPosition(node, 'bottom');
```

## 🔄 Миграция

### Текущее состояние

- `FlowchartEditor.tsx` - использует `app.js` через динамическую загрузку скрипта
- `FlowchartEditorRefactored.tsx` - новая версия с использованием модулей (в разработке)

### План миграции

1. ✅ Создать все модули (выполнено)
2. 🔄 Интегрировать в FlowchartEditor.tsx
3. ⏳ Тестирование
4. ⏳ Удаление старого app.js (после полного перехода)

## 📝 Основные модули

### Парсеры

- **pascalParser.ts** - Парсит Pascal SPR формат в блок-схему
- **cParser.ts** - Парсит C/C++ AST формат в блок-схему
- **expressionConverter.ts** - Конвертирует AST выражения в читаемые строки

### State Management

- **FlowchartStore** - Класс для управления состоянием блок-схемы
- Поддержка подписок на изменения
- Методы для работы с узлами, соединениями, историей

### Рендеринг

- **connectionRenderer.ts** - Рендерит соединения на SVG/Canvas
- **nodeRenderer.ts** - Рендерит узлы на DOM
- **panelRenderer.ts** - Рендерит панели (info, comments, history)

### Обработчики

- **nodeHandlers.ts** - Создание, редактирование, перетаскивание узлов
- **connectionHandlers.ts** - Создание соединений между узлами
- **canvasHandlers.ts** - Panning и zoom canvas

## 🔧 Типы

Все основные типы определены в:

- `types/flowchart.ts` - FlowchartNode, Connection, FlowchartState, etc.
- `types/parser.ts` - PascalProgram, ASTProgram, все AST типы

## 📚 Документация модулей

Каждый модуль содержит JSDoc комментарии для основных функций.

## 🐛 Отладка

Используйте консоль браузера для отладки. Все модули логируют важные события с префиксом `[DEBUG]`.

