# Инструкция по импорту проекта в Git репозиторий

## 📦 Подготовка к первому коммиту

### 1. Проверка текущего состояния

```bash
# Проверьте, что вы в корне проекта
cd Z:\Coding\DAoSS

# Проверьте статус Git
git status
```

### 2. Убедитесь, что .gitignore настроен правильно

Основной `.gitignore` уже создан в корне проекта. Он исключает:
- ✅ Все `bin/` и `obj/` папки (.NET)
- ✅ Все `build/` папки (Android)
- ✅ `appsettings.Development.json` (с паролями)
- ✅ `local.properties` (Android)
- ✅ Временные файлы и кэши

### 3. Проверьте, что чувствительные данные не попадут в репозиторий

**Важно:** Убедитесь, что файл `appsettings.Development.json` содержит только шаблонные значения:

```bash
# Проверьте содержимое
cat DAoSS-backend_wip/src/WebApi/appsettings.Development.json
```

Должно быть:
- `"Password": "CHANGE_ME"` (не реальный пароль)
- `"Key": "CHANGE_ME_TO_RANDOM_STRING..."` (не реальный ключ)

### 4. Инициализация Git репозитория (если еще не инициализирован)

```bash
# Если репозиторий еще не инициализирован
git init

# Добавьте remote (замените на ваш URL)
git remote add origin https://github.com/your-username/DAoSS.git
# или
git remote add origin git@github.com:your-username/DAoSS.git
```

## 📝 Первый коммит

### 1. Добавьте все файлы (кроме игнорируемых)

```bash
# Добавьте все файлы
git add .

# Проверьте, что будет добавлено (должны быть исключены bin/, obj/, build/, appsettings.Development.json)
git status
```

### 2. Создайте первый коммит

```bash
git commit -m "Initial commit: Backend (.NET 8) and Android client setup"
```

### 3. Отправьте в удаленный репозиторий

```bash
# Если это первый push
git push -u origin main
# или
git push -u origin master

# Для последующих коммитов просто
git push
```

## 🔄 Работа с репозиторием

### Типичный workflow

```bash
# 1. Проверьте изменения
git status

# 2. Добавьте измененные файлы
git add .

# 3. Проверьте, что добавляется (особенно важно для первого раза)
git status

# 4. Создайте коммит
git commit -m "Описание изменений"

# 5. Отправьте изменения
git push
```

### Что НЕ должно попадать в репозиторий

❌ **Никогда не коммитьте:**
- `appsettings.Development.json` с реальными паролями
- `local.properties` (Android)
- Файлы в `bin/`, `obj/`, `build/`
- Личные настройки IDE (`.idea/`, `.vs/`)
- SQL дампы с реальными данными
- Файлы с паролями и ключами

✅ **Всегда коммитьте:**
- Исходный код (`.cs`, `.java`, `.xml`)
- Конфигурационные файлы-шаблоны (`appsettings.Development.example.json`)
- Миграции Entity Framework
- `.gitignore`
- `README.md`, `SETUP.md`
- `build.gradle`, `*.csproj`

## 🛡️ Проверка перед коммитом

### Быстрая проверка:

```bash
# Проверьте, что нет чувствительных данных
git diff --cached | grep -i "password\|secret\|key" | grep -v "CHANGE_ME"

# Проверьте размер добавляемых файлов (не должно быть больших бинарных файлов)
git diff --cached --stat
```

### Проверка .gitignore:

```bash
# Убедитесь, что игнорируемые файлы действительно игнорируются
git status --ignored
```

## 📋 Чеклист перед первым push

- [ ] `.gitignore` настроен и работает
- [ ] `appsettings.Development.json` содержит только шаблонные значения
- [ ] Нет реальных паролей в коммитах
- [ ] `appsettings.Development.example.json` создан как шаблон
- [ ] Все `bin/`, `obj/`, `build/` папки игнорируются
- [ ] `SETUP.md` создан с инструкциями
- [ ] README файлы актуальны

## 🔐 Безопасность

### Если случайно закоммитили пароли:

```bash
# 1. Удалите файл из истории Git (ОСТОРОЖНО!)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch DAoSS-backend_wip/src/WebApi/appsettings.Development.json" \
  --prune-empty --tag-name-filter cat -- --all

# 2. Или используйте BFG Repo-Cleaner (рекомендуется)
# Скачайте с https://rtyley.github.io/bfg-repo-cleaner/
bfg --replace-text passwords.txt

# 3. Force push (только если вы единственный разработчик!)
git push origin --force --all
```

**⚠️ Внимание:** Force push перезаписывает историю. Используйте только если уверены!

### Лучшая практика:

1. Используйте `.env` файлы или переменные окружения
2. Храните секреты в отдельном репозитории или менеджере секретов
3. Используйте `git-secrets` для автоматической проверки

## 📚 Полезные команды Git

```bash
# Просмотр истории коммитов
git log --oneline

# Просмотр изменений в файле
git diff filename

# Отмена изменений в рабочей директории
git checkout -- filename

# Отмена добавления файла (перед коммитом)
git reset HEAD filename

# Просмотр удаленных репозиториев
git remote -v

# Обновление из удаленного репозитория
git pull origin main

# Создание новой ветки
git checkout -b feature/new-feature

# Переключение между ветками
git checkout main
```

## 🆘 Проблемы и решения

### "Changes not staged for commit"

```bash
# Добавьте файлы
git add .
git commit -m "Your message"
```

### "Untracked files"

Проверьте `.gitignore` - возможно, нужно добавить паттерн для игнорирования.

### "Large files detected"

GitHub имеет лимит 100MB на файл. Если нужно добавить большие файлы:
- Используйте Git LFS: `git lfs track "*.largefile"`
- Или исключите файл через `.gitignore`

---

**Готово! Теперь ваш проект готов к совместной работе. 🚀**

